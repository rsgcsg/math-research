# 当前状态：完整双自由旋转宿主恰五色，原始HN未解决

2026-09-26。对齐主线ca2d487…与分歧研究分支482b530…；完整来源和旧编号映射见
[归并清单](../certificates/rotation_integration_provenance.json)。主线既有成果不被旧分支覆盖。
**普通界仍为5≤χ(R²)≤7。新增归并T135–137/E106–109；本轮实际新推进为T138/E110，Q011已解决。**
[完整新证明](proofs/rank2_rotation_five_coloring.md) · [账本](RESULTS.md) · [路线](ROUTES.md) ·
[统一框架](proofs/hn_unified_framework.md) · [文献地图](../references/LITERATURE_MAP.md)。

## 1. 本轮不是只整理：已补完双旋转的全部关系和无限五染

令H′=〈η,u,τ〉≅C₅×Z²，Y为10077点/49858实际边的原配置。
旧分支只给出接触范围855 gain、识别范围325 gain和自边校准；现在已全部算完：
2082非零轨道、81410返回、11261反向去重单位接触、30原点邻轨。
32维生产器与16维独立检查器以不同素数筛选和不同轨道算法得到同一全表；
独立检查还重算2/5处的精确赋值及无限尾界，未把有限窗口当完整范围。

保存词a_i和P=(123)给公式：
`c(0)=0, c(η^h u^n τ^k r_i)=P^n(a_i)`，P固定颜色0和4。
全部接触和原点边直接通过；原Y的非四染子图给下界，所以χ(U(H′Y))=5。
三条真实u²自接触排除标号周期1/2，公式给周期3。
**Q011关闭；任意更多同宿主正负旋转层不能给NON5。**
不将一般Z²有限型移位可行性改写为周期可行性；本结果是显式的周期正证据。

## 2. 推广已经检验，仍缺的共同律没有被掩盖

该词修复τ/η/u，却在另外12个旧运动上失配。
例如平移1把点对32/119映到876/1718，源、像同色真假不同。
15个完整最大域由独立旧检查器重新构造；详细反例在新证明第5节和
[边界证书](../certificates/rank2_joint_boundary.json)。
全局H′相移仅是全局颜色重命名，所以这一个保存族的任意混合也无法修复12项；
**这不否定H′Y的其他染色，也不否定全Y上的其他共同律。**

自由15域问题仍是：全部合法五色划分Ω₅(Y)上存在同一个λ≥0、Σλ=1，
满足Dλ=0，其中D按每个运动的完整源/像划分差构造。
支持、权重、词和匹配都可变化。E084/E097/E099/E105的UNKNOWN全部保留。
E105已经实际生成30个反例列；三个最终词池的支持仍删空，不是全词反证。
其通用UNSAT独立认证后端仍未完成，没有现成全词UNSAT只等补认证。

## 3. 本次对齐纠正了重复编号和旧状态

482b530…从旧efd931ee…分叉，旧T125–128/E095–098与main已占用编号冲突。
旧T125与main已有T129重复，保留其另证而不新增定理；其余映射为T135–137和E106–109。
16份来源Python/JSON原字节保留，输出中的旧标识用“来源提交＋编号”解释。
6份证明的主线显示编号已映射，所有main既有研究源/证据均保留。
不再传播旧分支CURRENT中“仅本地、未推送”的过期说法；本轮发布以远端提交为准。

已归并的旧分支成果：九点非CM例的全部有理旋转饱和χ4；整个〈η,u〉Yχ5；
满秩赋值编译多旋转接触的有限候选。这些不是本轮新发现。
main此前T125–134及E105继续有效，旧T129已有更紧的二候选单旋转论证。

## 4. 真正瓶颈与下一条值得投入的义务

**主攻仍是完整自由颜色关系。** 新增生成元的独立性和完整关系表已经解决了Q011，
却没有产生不相容。下一有效候选必须真正改变完整颜色关系，而非增加同H′指数窗口。
可以用配套列同时平衡全部新模式，或让共享事件势覆盖未见模式；负结论必须对全Ω₅(Y)成立。
正解只退休当前有限实例，不证明全平面五染。

**几何备线须超出固定半径旋转闭包，或提供新无条件接口限制。** H′保持原1259个非零半径；
任意非零平移会产生无限多新半径，新证明第6节给出二次方程的直接论证。
这说明平移闭包不是再加一个相位，但不证明加入平移就升色。
不冻结刚得到的a_i去冒充自由五色；实际二维实现和全自由端口限制仍须同时成立。

**全局上界仍缺覆盖所有有限实代数配置的机制。** 本轮只给一个无限子宿主χ5；
它不是整个数域或整个平面的染色。固定精度、群阶、状态数和计算量都不是终局进度量。
最终答案5需全局≤5；6需NON5和全局≤6两项；7需NON6，不预定答案。

## 5. 验证与历史范围

`make check-rotations`重放导入的交换块、有理旋转和整个η/u链；
`make check-rank2`重建整个新宿主、15域边界、拒绝篡改并逐字节重建新证书。
后者只需标准库，搜索的SAT依赖不进入独立检查器。
[本轮来源和回归收据](../certificates/rotation_integration_validation.json)分别记录各次运行和冻结源码；
不以任何旧PASS冒充新增全仓运行，不把有限验证说成全体定理的形式化或外部同行评审。

E004/部分旧轨道原包和G29巨大展开仍保留原未恢复/未重放状态。
本轮文献复核仍未提供普通HN新界；同名rank-two Cayley文献的rank是关系矩阵列数，
不能与本页两个乘法生成元混用。周期染色文献的局部有限与有界pathwidth假设也不能省略。
