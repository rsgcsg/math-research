# 当前研究状态

2026-09-21；接续远端 `5cd6b56373e9e92d686101a87028e213747feaf2`。
当前研究分支 `research/cyclic-orbit-20260921`；没有改动或强制合并 main。
**原始 Hadwiger–Nelson 未解决，本轮没有普通平面色数新界。**
[完整证明](proofs/dyadic_cyclic_orbit.md) · [统一框架](proofs/hn_unified_framework.md) ·
[路线](ROUTES.md) · [结果账本](RESULTS.md)。

## 本轮决定了一个真实无限宿主，而不只是增加有限正例

**T130 / E100：O=∪_(n∈Z)uⁿY 的全部实际单位距离图恰五色。**
Y仍是10077点、49858条全单位边的既有点集，新u仍为T116/E084的二进逃逸旋转。
三个完整五色词给出 `c(uⁿp_i)=w_(n mod3)(i)`。独立检查证明该式在所有重合
点上良定义，并覆盖所有跨层单位边，因而是整个实际无限O的染色。
不只检查有限层，也不把组合周期商当作实际有限单位实现。

**全部幂次已被有效穷尽。** 非零点的一个2进赋值范围为−2至4，v(u)=−1，
故非零返回只能在 |n|≤6。完整正返回域大小为29、805、5，随后所有 n≥4
都只含原点。非原点的单位接触代表在层差0、1、2、3、4分别有
49498、938、12379、126、188条；所有 n≥5均无接触。另有360个原点邻点
在每层产生单位边。原点固定及其无限星已单独覆盖。

**最小标号周期恰为3。** 三周期商有27745点、143817边并给出正词；
Y内索引597的点 `p=η²(ν−2/3−h)` 满足 `|u²p−p|²=(1−h)(2+h)=1`。
因而任何周期1或2的染色都会把这条实际边染同色，与颜色数无关。
这不是 SAT 受限负返回，也不是最小概率支持数结论。

**T129：把单旋转的无限量词压成可计算有限状态。** 给定v(u)≠0，
`|uⁿp−q|=1` 的三项范数关系至多给两个候选整数n；随后精确验算。
全部实际重合与边因此能有限列出，再构成真实循环轨道的有限记忆系统。
其可k染当且仅当存在某个有显式粗界的标号周期解；固定几个小周期失败仍不完备。
对于没有这种有限素点的代数单位旋转，某个阿基米德共轭的模长不为1，
三角不等式给可计算的双向指数尾界。因此结论覆盖所有给定的代数单旋转轨道，
但不提供对任意P的统一规模界，也不决定整个平面。

## 正结果的边界：旧14共同律没有被修复

均匀选择上述三相，在整个O上得到对所有uⁿ同时不变的完整五色联合律。
它不仅满足有限Y的高次返回，还对应一个真正的无限实际染色。
**独立审计旧14域：14项全部失配。** 所以旧14+u的E084/E097/E099仍是
各自历史UNKNOWN，不能把全部u幂正律与旧14正律擅自拼起来。
原E092有限Y上的u/u²身份词没有失效；它只是不具备本次O的全接触延拓。

现已停止以“在同一Y上继续增加u幂次”寻找普通NON5；全O上界已经排除它。
可继续研究的是真正混合的多个运动，尤其带平移的共同义务，或者其他实际几何。
不存在本轮的有限NON5、全局≤6或不可判定性证明。

## 保留的有效基础和未知量词

T127–128 / E098：同m≤5的等权五色划分由四点信息决定；任意权、每份≤5
支持由五点决定，两界都尖锐。T114只保证固定可行joint有某个有限支持，
不保证五个，所以五词模型的失败仍不能作为全proper五色空间的分离。
E097、E099改进了编码，但旧14+u都没有正证书，UNKNOWN原样保留。

T125–126 / E095–096：六次Salem宿主χ4，114点267边临界四色见证，及一般
阿贝尔Cayley三色高度判据仍有效。它们不是本轮成果，也没有升级为五色高度法。
T123非CM九点例仍χ4且不增旧端口关系；域外临界核须过其接触/列表延拓筛选。
T122的实数域共尾量词没有被本次单循环宿主定理绕过。
Q010（半层/四分之一层R₂是否≤5）与H29非线性≤6仍开放；不从O⊂L推断整个L可五染。

## 三条主线的最新入口

| 主线 | 下一项真正需要的结果 | 已关闭的替代路线 |
|---|---|---|
| 完整共同律 | 多运动的共同自由色词/律，或全部proper空间的严格分离 | 仅增加u幂、固定旧表示、单算子微修、五词预算加码 |
| 实际几何 | 通过域外临界核筛选、带完整自由五色障碍的新实例 | 只逃数域、低度附加层、不可实现的抽象高色模板 |
| 上界 | 真正覆盖全部所需实数域的≤6构造或严格零坏边极限 | 当前循环O、固定域/固定有限精度正例冒充全平面 |

## 验证和工作区

新增独立运行 `35551030874` 已完整成功；其受检研究提交为`e302bfdd…`，
证书随后写入`6a350e064…`。两套算术分别用32/16维基、不同局部多项式、
16/24位精度、不同过滤素数及不同候选推导，最后均作精确整数范数判断。

[独立收据](../certificates/dyadic_cyclic_orbit_validation.json) 为PASS：
190467项边颜色义务、全部高次重合、商图点边数、14种篡改拒绝、-O拒绝，
以及旧14逐项完整划分审计。首次生产运行计算成功但发布步骤认证方式错误；
修正后按相同程序重建，证书SHA256仍为
`79d1448921c3d66604d847d617dec496245e82393a3b7a8ea373ae2c9af3a172`。

全仓回归状态：待本分支完整工作流收据；已通过的是E100新增独立验证。
`make check-orbit` 是新增独立入口，`make check` 保留原有全部入口并串入它。
一般T129依赖完整数学证明，不以有限测试代替；Q(i)校准另检查24025种点对/幂次。
本地已收到真实证书归档，完整工作区以工作流导出的精确源快照与Git元数据为准，
不把此前仅有增量文件的环境描述成完整clone。
